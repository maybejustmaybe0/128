-- 128's Booster (editado: movible con dedo + tamaño chiquito para móvil)
-- free script https://discord.gg/RXUs6d3yxk

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local ProximityPromptService = game:GetService("ProximityPromptService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local colors = {
    mainBg = Color3.fromRGB(12, 12, 17),
    buttonBg = Color3.fromRGB(25, 25, 30),
    accent = Color3.fromRGB(114, 137, 218),
    text = Color3.fromRGB(200, 200, 200),
    textActive = Color3.fromRGB(255, 255, 255)
}

-- Limpieza previa
pcall(function()
    CoreGui:FindFirstChild("128Booster"):Destroy()
end)

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "🌩️ 128 Booster"
screenGui.ResetOnSpawn = false
screenGui.Parent = CoreGui  -- CoreGui para que sea visible siempre

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 240, 0, 200)  -- Chiquito para móvil
mainFrame.Position = UDim2.new(0.5, -120, 0.5, -100)  -- Centrado
mainFrame.BackgroundColor3 = colors.mainBg
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true  -- Movible con dedo!
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = mainFrame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 85, 0)
stroke.Thickness = 2.5
stroke.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 40)
title.Text = "⚡ 128's booster"
title.Font = Enum.Font.GothamBold
title.TextColor3 = colors.textActive
title.TextSize = 18
title.BackgroundTransparency = 1
title.Parent = mainFrame

local speedBtn = Instance.new("TextButton")
speedBtn.Size = UDim2.new(0.9, 0, 0, 45)
speedBtn.Position = UDim2.new(0.05, 0, 0.24, 0)
speedBtn.BackgroundColor3 = colors.buttonBg
speedBtn.Text = "Speed Boost: 31"
speedBtn.Font = Enum.Font.GothamMedium
speedBtn.TextColor3 = colors.text
speedBtn.TextSize = 14
speedBtn.Parent = mainFrame
Instance.new("UICorner", speedBtn).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", speedBtn).Color = colors.buttonBg

local potionBtn = Instance.new("TextButton")
potionBtn.Size = UDim2.new(0.9, 0, 0, 45)
potionBtn.Position = UDim2.new(0.05, 0, 0.47, 0)
potionBtn.BackgroundColor3 = colors.buttonBg
potionBtn.Text = "Potion Steal: 37.6"
potionBtn.Font = Enum.Font.GothamMedium
potionBtn.TextColor3 = colors.text
potionBtn.TextSize = 14
potionBtn.Parent = mainFrame
Instance.new("UICorner", potionBtn).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", potionBtn).Color = colors.buttonBg

local autoStealBtn = Instance.new("TextButton")
autoStealBtn.Size = UDim2.new(0.9, 0, 0, 45)
autoStealBtn.Position = UDim2.new(0.05, 0, 0.7, 0)
autoStealBtn.BackgroundColor3 = colors.buttonBg
autoStealBtn.Text = "AUTO STEAL: OFF"
autoStealBtn.Font = Enum.Font.GothamMedium
autoStealBtn.TextColor3 = colors.text
autoStealBtn.TextSize = 14
autoStealBtn.Parent = mainFrame
Instance.new("UICorner", autoStealBtn).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", autoStealBtn).Color = colors.buttonBg

-- Variables
local speedBoostEnabled = false
local potionStealEnabled = false
local autoStealEnabled = false

-- Conexiones
local connections = {}

-- Speed Boost (31)
RunService.Heartbeat:Connect(function()
    if not speedBoostEnabled then return end
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if hrp and hum and hum.MoveDirection.Magnitude > 0 then
        local ts = 31
        local tv = Vector3.new(hum.MoveDirection.X, 0, hum.MoveDirection.Z) * ts
        hrp.Velocity = hrp.Velocity:Lerp(Vector3.new(tv.X, hrp.Velocity.Y, tv.Z), 0.45)
    end
end)

-- Potion Steal (37.6)
RunService.Heartbeat:Connect(function()
    if not potionStealEnabled then return end
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if hrp and hum and hum.MoveDirection.Magnitude > 0 then
        local ts = 37.6
        local tv = Vector3.new(hum.MoveDirection.X, 0, hum.MoveDirection.Z) * ts
        hrp.Velocity = hrp.Velocity:Lerp(Vector3.new(tv.X, hrp.Velocity.Y, tv.Z), 0.45)
    end
end)

-- Auto Steal
local autoStealLoop
autoStealBtn.MouseButton1Click:Connect(function()
    autoStealEnabled = not autoStealEnabled
    autoStealBtn.Text = "AUTO STEAL: " .. (autoStealEnabled and "ON" or "OFF")
    autoStealBtn.BackgroundColor3 = autoStealEnabled and Color3.fromRGB(114, 137, 218) or colors.buttonBg
    autoStealBtn.TextColor3 = autoStealEnabled and Color3.white or colors.text
    
    if autoStealEnabled then
        autoStealLoop = task.spawn(function()
            while autoStealEnabled do
                for prompt in pairs(ProximityPromptService.Prompts) do
                    if prompt and prompt.Parent and prompt.Enabled then
                        if string.find(string.lower(prompt.ActionText .. prompt.ObjectText), "steal") then
                            fireproximityprompt(prompt, 1)
                            if potionStealEnabled then
                                task.wait(0.1)
                                local bp = LocalPlayer:FindFirstChild("Backpack")
                                local char = LocalPlayer.Character
                                if bp and char then
                                    local tool = bp:FindFirstChild("Giant Potion") or char:FindFirstChild("Giant Potion")
                                    if tool then
                                        char.Humanoid:EquipTool(tool)
                                        task.wait(0.05)
                                        VirtualInputManager:SendMouseButtonEvent(0,0,0,true,game,0)
                                        task.wait(0.05)
                                        VirtualInputManager:SendMouseButtonEvent(0,0,0,false,game,0)
                                    end
                                end
                            end
                        end
                    end
                end
                task.wait(0.1)
            end
        end)
    else
        if autoStealLoop then task.cancel(autoStealLoop) autoStealLoop = nil end
    end
end)

-- Speed Boost toggle
speedBtn.MouseButton1Click:Connect(function()
    speedBoostEnabled = not speedBoostEnabled
    speedBtn.BackgroundColor3 = speedBoostEnabled and Color3.fromRGB(114, 137, 218) or colors.buttonBg
    speedBtn.TextColor3 = speedBoostEnabled and Color3.white or colors.text
end)

-- Potion Steal toggle
potionBtn.MouseButton1Click:Connect(function()
    potionStealEnabled = not potionStealEnabled
    potionBtn.BackgroundColor3 = potionStealEnabled and Color3.fromRGB(114, 137, 218) or colors.buttonBg
    potionBtn.TextColor3 = potionStealEnabled and Color3.white or colors.text
end)

print("Hub cargado - Menú chiquito movible abierto automático")
